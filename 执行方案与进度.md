# DSH Desktop 执行方案与进度

> 创建：2026-08-21 ｜ 来源：全项目遍历审查 + 更新问题取证 + 手机 App 规划
>
> **使用方式**：每条任务前的状态标记手动维护——`⬜ 待办` `🔄 进行中` `✅ 完成` `❌ 取消`。
> 完成或变更时在文末「进度日志」追加一行。让 AI 继续干活时，让它先读本文档。

---

## 总览

| 优先级 | 板块 | 目标 |
|---|---|---|
| **P0** | A. 更新链路修复 | 解决"下载了但没实际更新"，源头到客户端闭环 |
| **P0** | B. 安全加固 | 堵住局域网暴露与 IPC 面两大洞 |
| **P1** | C. 屎山清理 | 死代码、注释谎言、调试残留、重复逻辑 |
| **P2** | D. 手机 App 连接 | 同 WiFi 手机可发现、可鉴权、可操作 |

依赖关系：A 是 D 的地基（手机连的服务必须能可靠升级）；B 的鉴权是 D 上线的前置条件。

---

## P0-A 更新链路修复

### A1. ✅ 验证 GitHub Release 资产是否滞后（取证，约 10 分钟）
- **现象依据**：本机 runtime 仍为 `0.1.0-rc.7`，npm latest 为 `0.1.1-rc.1`；最近两次 `do_update invoked` 无报错但版本未变 → 高度怀疑 Release `runtime` tag 上的 zip 内容就是旧包
- **动作**：
  - [x] 查仓库 Actions 页 sync-runtime 最近运行状态（绿/红）
  - [x] 下载 `https://github.com/fanqie132/dsh-desktop/releases/download/runtime/runtime.zip`，核对内部 `@deepseek-ai/dsh/package.json` 版本号
- **验收**：明确写出根因结论（资产滞后 / CI 失败 / 其他）
- **✅ 结论（2026-08-21）**：根因坐实，双重证据——
  1. sync-runtime workflow 最近两次运行（08-20、08-21 定时触发）conclusion 均为 **failure**，资产从未自动更新；
  2. 实际下载 Release 资产（74,414,683 bytes），内部 dsh 版本 = **0.1.0-rc.7** ≠ npm latest 0.1.1-rc.1。
  客户端整条更新链路（检测→下载→替换）工作正常，坏在源头资产不更新。
  附注：CI 具体失败 step 因 gh 未安装、匿名 API 限流、token 无 Actions 权限未能远程查明，留 A2 本地修复时一并看日志。

### A2. ⬜ 修 sync-runtime.yml 可靠性
- **已知隐患**：`Compress-Archive` 对数万小文件的 node_modules 极慢且有 260 字符路径限制；上传失败静默；失败无通知
- **动作**：
  - [ ] 打包改用 `tar -a -cf runtime.zip -C .. runtime`（README 已推荐的命令）
  - [ ] 确认 CI 中 pnpm 与本地同为 hoisted 结构（`runtime/pnpm-workspace.yaml` 已入库即可）
  - [ ] 上传后加回读校验：重新下载资产、解压核对版本 == npm latest，不一致 fail
  - [ ] 加 failure 通知（开 issue 或其他渠道）
- **验收**：手动 workflow_dispatch 跑一次全绿，Release 资产版本与 npm 一致

### A3. ⬜ 客户端更新结果自检闭环
- **问题**：`do_update` 成功路径零日志、零校验，出现"假成功"
- **位置**：`src-tauri/src/updater.rs:182` do_update
- **动作**：
  - [ ] 替换完成后回读新 package.json 版本，与目标版本比对，不一致按失败处理并提示
  - [ ] 更新窗口显示「rc.7 → rc.9 更新完成」类可感知文案
  - [ ] 成功也写一条 updater 日志（现在只有失败才写）
- **验收**：人为放置旧 zip 时，客户端报错而不是静默

### A4. ⬜ 删除死代码 update_runtime()
- **问题**：零调用点，do_update 内联重复实现了同一逻辑
- **位置**：`src-tauri/src/updater.rs:105-134`
- **验收**：删除后 cargo check 通过

---

## P0-B 安全加固

### B1. ⬜ forwarder 加配对鉴权（手机规划的前置，不做不许暴露局域网）
- **问题**：8787 端口对同 WiFi 零鉴权开放，任何人可操作你的 AI 会话与审批
- **位置**：`src-tauri/forwarder.cjs` 全文
- **动作**：
  - [ ] 首次启用生成随机 token，电脑端弹二维码展示
  - [ ] 请求校验 token（header 或 URL 参数），不带返回 403
  - [ ] WebSocket upgrade 同样校验
- **验收**：无 token 的设备访问被拒

### B2. ⬜ CSP 与 IPC 面收敛评估
- **问题**：`tauri.conf.json` 中 `security.csp: null` + `withGlobalTauri: true` + remote url 白名单（`src-tauri/capabilities/default.json:7`），远程页面可 invoke IPC
- **动作**：
  - [ ] 评估最小 capability 集：updater 窗口保留 do_update，主窗口（加载远程内容）仅保留 save_image 所需权限
  - [ ] 尝试为壳自身页面设置基础 CSP（远程内容不受 CSP 控制但壳页面受控）
- **验收**：capability 列表逐项有存在理由

### B3. ⬜ forwarder 开关 + 禁止 HTTP 明文回退
- **问题**：无条件监听 0.0.0.0:8787；证书生成失败时静默回退 HTTP 明文对局域网开放
- **位置**：`src-tauri/src/dsh.rs:315` spawn_forwarder、`forwarder.cjs:80`
- **动作**：
  - [ ] 加托盘菜单/配置项开关，默认关
  - [ ] 证书不可用时不上线转发器（而不是回退 HTTP）
- **验收**：默认安装后 8787 不监听

### B4. ⬜ 修证书不随 IP 变化重建（注释谎言 bug）
- **问题**：注释说"IP 变化时重新生成"，实现只查 `pfx.exists()` 就复用；DHCP 换 IP 后手机端证书报错
- **位置**：`src-tauri/src/dsh.rs:250-275` ensure_forwarder_cert
- **动作**：
  - [ ] 记录生成时的 IP（旁车文件如 `forwarder.ip.txt`），IP 变化则删旧重签
- **验收**：改本机 IP 后重启应用，pfx 重新生成且 SAN 含新 IP

---

## P1-C 屎山清理

### C1. ⬜ 清理 %TEMP% 散落调试日志，统一日志入口
- **清单**：`lib.rs:41` log_dbg 写 dsh-saveimage.log；`lib.rs:165` on_page_load 写 dsh-pageload.log；updater.rs 三处直写 dsh-updater.log / dsh-updater-update-error.log；runtime.rs 写 dsh-runtime-download-curl.log
- **动作**：
  - [ ] 抽一个统一的 log 函数（级别 + 单文件 + 追加），替换所有直写点
  - [ ] 删除 pageload/saveimage 这类纯调试日志
- **验收**：%TEMP% 新增日志只有 1 个，含时间戳与级别

### C2. ⬜ restart() 锁内长操作外移 + spawn_dsh 去 expect
- **问题**：`restart()` 持 Mutex 期间最多阻塞 10s（wait_port_free），期间 start/stop 全卡；`spawn_dsh` 多处 expect 在锁内 panic 会毒化锁
- **位置**：`src-tauri/src/dsh.rs:145-158, 161-190`
- **动作**：
  - [ ] wait_port_free 移到锁外（先 drop 锁再等待再重加锁，或重构为状态机）
  - [ ] 启动失败改为返回 Result 并上报 UI，不 panic
- **验收**：restart 期间调用 stop 不阻塞；模拟 node 缺失时应用不崩

### C3. ⬜ sound.js 轮询降频 + 可关闭
- **问题**：每秒 POST `/api/session.list` 常驻开销，无开关
- **位置**：`src-tauri/sound.js:127`
- **动作**：
  - [ ] 轮询间隔提到 3~5s（完成音延迟感知可接受）
  - [ ] 预留 localStorage 开关位，后续接设置界面
- **验收**：网络面板可见请求频率下降

### C4. ⬜ tray.rs 与 updater.rs 重复逻辑抽取
- **问题**：「轮询 3080 就绪 → show + reload」两处各写一份
- **位置**：`src-tauri/src/tray.rs:51-67`、`src-tauri/src/updater.rs:137-151`
- **动作**：抽公共函数（如 `wait_dsh_ready_and_reload(app, timeout)`）
- **验收**：行为不变，单一实现

### C5. ⬜ 模板残留清理
- **清单**：
  - [ ] `Cargo.toml:4-5` description="A Tauri App"、authors=["you"]
  - [ ] `src/assets/` 下 tauri.svg / typescript.svg / vite.svg 脚手架图（确认无引用后删）
- **验收**：cargo check + pnpm build 通过

### C6. ⬜ 根目录散落文件清理（⚠️ 删除前需确认）
- **清单**：build.log、build2.log、build2-err.log、dev.log、dsh.log、runtime.zip（78MB）
- **说明**：均已 gitignore，纯占磁盘；runtime.zip 若还要手动发布可留
- **验收**：列出将删清单 → 用户确认 → 删除

### C7. ⬜ tokio 依赖移除评估
- **问题**：仅用了 `spawn_blocking` 一处（`lib.rs:21`），std::thread 即可，白增编译时间
- **动作**：确认无其他使用点后移除依赖，改 std thread
- **验收**：cargo check 通过，Cargo.lock 减负

---

## P2-D 手机 App 连接（前置：B1、B4 完成）

### D1. ⬜ 连接发现机制
- **动作**：
  - [ ] 方案一（简）：托盘菜单「手机连接」弹二维码窗口（内容含 IP + 端口 + token）
  - [ ] 方案二（全）：mDNS 广播（Rust `mdns-sd` crate），App 端自动发现
- **建议**：先做方案一验证，mDNS 后补
- **验收**：新手机扫码即连，无需手输 IP

### D2. ⬜ 电脑侧常驻保障
- **动作**：
  - [ ] 开机自启开关（tauri-plugin-autostart，托盘菜单挂开关）
  - [ ] 确认关窗到托盘 + 托盘常驻在自启场景下表现正常
- **验收**：重启电脑后 dsh 服务自动可用

### D3. ⬜ 手机端载体：PWA 先行验证
- **理由**：UI 由 DeepSeek 官方维护，App 层越薄跟随成本越低
- **动作**：
  - [ ] 用手机浏览器实测完整操作链路：对话流式输出、审批弹窗、图片显示、长会话
  - [ ] 记录移动端体验问题清单（视口、键盘、触控目标大小）
- **验收**：产出「PWA 可行性结论」写入进度日志

### D4. ⬜ Tauri Mobile 壳决策（依 D3 结论）
- **条件**：PWA 体验不达标（如 iOS 对自签证书/PWA 限制多）才启动
- **动作**：Tauri 2 Android/iOS 壳，WebView 直指 `https://<ip>:8787`，内置信任自签证书逻辑
- **验收**：手机 App 安装后扫码配对可用

### D5. ⬜ 打磨（远期）
- [ ] 断线重连提示
- [ ] 审批提醒推送到手机通知栏
- [ ] 息屏后台保活策略

---

## 附：已确认无需处理的项

| 项 | 结论 |
|---|---|
| os error 5 文件占用（历史 fix） | git log 显示已两轮修复收敛，A3 自检上线后可彻底兜底 |
| zip crate 0.6 旧版 | 功能正常，升级随缘，不单独立项 |

---

## 进度日志（倒序追加）

| 日期 | 任务 | 动作 | 结果 |
|---|---|---|---|
| 2026-08-21 | — | 全项目遍历审查 + 更新问题取证 + 本方案建立 | 文档就绪，待启动 A1 |
| 2026-08-21 | A1 | 查 Actions 运行记录 + 下载 Release 资产核对版本 | ✅ 根因坐实：CI 连续 failure + 资产停在 rc.7，客户端链路正常。下一步 A2 |
